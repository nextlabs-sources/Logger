package com.nextlabs.logger.util;

public interface LogUtility {
	public static enum LogLevel {
		ERROR, WARN, INFO, DEBUG, TRACE
	}

	public static enum AccountType {
		PERSONAL, PROJECT, ENTERPRISEWS
	}
}
